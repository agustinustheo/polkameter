# Reference

These pages are a compact contract reference for scenario authors, CI maintainers, and remote-agent operators. They describe the current versioned interfaces; use `polkameter --help` and preflight output when diagnosing a particular installed binary or runtime.
